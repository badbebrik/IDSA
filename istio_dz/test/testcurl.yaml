apiVersion: v1
kind: Pod
metadata:
  name: test-curl
  namespace: muffin
  labels:
    app: test-curl
spec:
  serviceAccountName: default
  restartPolicy: Never
  containers:
    - name: curl
      image: curlimages/curl:8.6.0
      command: ["sh", "-c", "sleep 3600"]
